# Admin surfaces

Admin UI uses the same Academia identity with higher information density. Preserve strong hierarchy for filters, tables, pagination, bulk actions, status semantics, and destructive confirmations. Never rely on color alone; pair status colors with text or icons and keep keyboard focus obvious.
