# Changelog

## 21 سبتمبر — تشخيص شامل: لماذا صفحات أدمن حقيقية تظهر فاضية + إصلاحات

- **السبب الجذري موثَّق بالكامل**: عشر ملفات `lib/*.functions.ts` (rbac، منهج، بلاغات،
  تقييم طالب، متابعة معلم، إشراف...) محمية عمدًا بـ`requireAuth` لحسابات الدخول السريع
  فقط (قرار أمان مقصود)، وكان 39 من 40 مستهلك لهالدوال يتجاهل فشل الجلب بصمت.
  التفاصيل والقائمة الكاملة: `docs/operations/2026-09-21-role-space-silent-failures-audit.md`.
- **13 ملف أُصلح هالجولة**: `modules-manager`، `roles-manager`، `role-permissions.$roleId`
  (+ Guard كان ناقص كليًا)، `admin.permissions`، `admin-dashboard`، `community-reports-manager`،
  `content-review-manager`، `teacher-verification-manager`، `curriculum-manager`،
  `curriculum-requests-manager`، `teacher.dashboard`، `supervisor.dashboard`، `parent.report`
  (كانت تعرض رسالة غلط "اربط أول ابن" بدل خطأ اتصال حقيقي).
- **القائمة الجانبية للأدمن**: صارت تعرض صفحات الإدارة فقط (`buildFullAdminAccess`)،
  بدون مساحات الطالب/المعلم/الإشراف الأكاديمي/ولي الأمر — الوصول المباشر بالرابط كان
  أصلًا محجوبًا (`ROLE_PAGE_PREFIXES.admin`)، هالتعديل يوقف تعارض القائمة مع الواقع فقط.
- **تحقّق شامل من Guard/403**: كل `<Guard pageKey>` بالمشروع (~120 مسار) يطابق سجل
  `PAGES` تمامًا، صفر تعارض. الاستثناء الوحيد (`role-permissions.$roleId` بلا Guard)
  أُصلح.
- **تحقيق خطأ تعديل صلاحيات المستخدم** ("Password required" عند التعديل): الكود الحالي
  بالباك اند (حتى بدون أي patch) فيه أصلًا `ModelState.Remove("Password")` الصحيح لحالة
  التعديل — الخطأ لا يُفسَّر من الكود المرفوع، الأرجح أن الخادم المنشور لسا ما تحدّث.
- **`bookings.ts` و`lessons.ts`**: طبقة ربط جاهزة (أنواع + دوال) بدون واجهة بعد — بانتظار
  P0-1/P1-1 (خطر عرض حجز حي ضد معلم بلا حساب مرتبط). أثناء بنائها تبيّن `apiClient` ما
  فيه `put` أصلًا رغم إن `Lesson/Update`/`ConfigureMeeting` بالباك اند `[HttpPut]` — أُضيفت.
- **تنظيف**: حذف `teacherPhotoPath`/`courseCoverPath` من `public-catalog-data.ts` (كود ميت
  مؤكَّد — استبدلناهم بتدرّج لوني/أيقونة بجولة ربط الكورسات). فحصت 24 تصدير آخر يبدو غير
  مستخدَم؛ أغلبها تحضير مقصود لميزات لسا ما لها واجهة (بنفس نمط bookings.ts/lessons.ts)
  وليس كودًا ميتًا، فتُركت بدون حذف افتراضي.
- **`docs/design/brand-guidelines.md`**: ملف جديد يوثّق قرار الشعار (لون ذهبي ثابت،
  مش يتبع الثيم) والـhex الدقيقة، مستخرج من تقريري 15 سبتمبر — كان `brand-logo.tsx` يشير
  لملف غير موجود.

## 21 سبتمبر — إتمام ربط ما كان جاهزًا بالباك اند وغير مربوط

- **صفحة جديدة `/course/$id`** (تفاصيل كورس) على `Course/GetById` الجاهزة أصلًا بدون واجهة.
  عنوان الكورس بـ`/courses` وبروفايل المعلم صار رابطًا إليها.
- **قسم "أوقات التوفّر"** ببروفايل المعلم على `Teacher/Availability` (عرض فقط، بدون حجز).
- تدقيق شامل على كل الـcontrollers أثبت أن كل شي تاني غير مربوط يعتمد فعليًا على P0-1 (صلاحيات)
  أو P1-1 (لا صف `Teacher` لحساب معلم) أو P1-3 (لا `CourseCategory` endpoint ولا بيانات) — موثّق
  بالتفصيل، ولم نلتفّ على أي منها.

## 21 سبتمبر — ربط تصفّح المعلمين والكورسات بالباك اند (المرحلة 1)

- **الكتالوج الحقيقي**: `/courses` صار على `Course/GetPublished` (العام) بشكل الرد المسطّح الفعلي
  (`teacherName`/`subjectName`)، بدل بيانات محلية. بنسخة الفرع الجانبي كان على `GetAll` (أدمن/معلم فقط)
  وعلى كيان متداخل، فصُحّح على كود الباك اند الحالي.
- **صفحة جديدة `/teachers`** (دليل المعلمين: بحث مؤخَّر + صيغة + "عرض المزيد") مع رابطها بالقائمة
  للزائر والطالب وولي الأمر، ومفاتيح ترجمة وSEO.
- **بروفايل المعلم حقيقي** (`/teacher/$id`): تقييم وسنوات خبرة ومؤهلات ولغات وأسعار وكورسات المعلم.
  أُزيلت أرقام وهمية كانت ثابتة لكل المعلمين (1,240 طالب، 4.8، "موثّق") ونصوص ترجمة ميتة.
- **حالات صادقة**: تحميل/خطأ اتصال مع إعادة محاولة/لا كتالوج بعد/لا نتائج. لا بيانات وهمية fallback.
  `getTeacherPublicProfile` و`getPublishedCourse` بيرجّعوا `null` لـ404 فقط.
- **Sitemap**: إضافة `/teachers` وإزالة 6 روابط معلمين وهمية.
- **اختبارات**: `courses.test.ts` و`teachers.test.ts` (12 سيناريو).
- **توثيق**: تقرير الربط، ومتطلبات الباك اند (`backend-requirements.md`، بما فيها ثغرة الصلاحيات P0-1)،
  وخارطة المتبقي وملاحظات المستقبل، وتحديث `frontend-integration-status.md` (61 endpoint).

## 21 سبتمبر — دمج تحسينات UI/UX من النسخة الفرعية (بنود #5 و#6 و#7 و#8)

- **#7 حالات التحميل**: ترحيل 41 ملفًا من `<Loader2 animate-spin>` الحر إلى
  `LoadingState` الموحّد (`role="status"`/`aria-live`) — 11 مكوّنًا أدمن/محفظة،
  وصفحة `teacher.$id`، و29 صفحة `_authenticated`. `payments-manager.tsx` (ملف ميت) حُذف بدل ترحيله — راجع بند "تنظيف".
- **#6 الطباعة**: utilities مسمّاة `text-nav` (13px) و`text-micro` (11px) بـ`styles.css`
  بدل `text-[13px]`/`text-[11px]` الحرة، مطبّقة بـ`app-sidebar` و`kit` و`courses` و`role-permissions`.
- **#8 RTL**: `left-0 right-0` → `inset-x-0` بـ`role-permissions.$roleId`، وتوثيق القاعدة.
- **#5 التحقق**: تحقق نموذج قبل الإرسال بـ`pages-manager` و`constants-manager`
  (نفس نمط `users-manager`).
- **اختبار**: `src/lib/onboarding.test.ts` (12 حالة، يقفل قاعدة "حساب قديم ما يتفاجأ بالـonboarding").
- **تنظيف**: حذف `src/hooks/use-sign-out.tsx` (نسخة قديمة بقيت بعد تقسيمه إلى
  `use-sign-out.ts` + `sign-out-overlay.tsx`)، وحذف `payments-manager.tsx` (شاشة CRUD وهمية
  غير مستخدمة)، وإزالة نحو 220 سطر كود معلّق من آخر `admin-dashboard.tsx`، وإعادة تجاهل
  `.env`/`.env.local` بـ`.gitignore`، وتوحيد كتابة اسم Academia بالتوثيق.
- **توثيق**: قسما "مقياس الطباعة" و"قواعد RTL/LTR" بـ`docs/design/design-system.md`،
  وأقسام 41–45 وقائمة "الباقي" بـ`Shared/full-project-report.md`.

## 19 سبتمبر — تسريع CI + تجهيز مكان السوشال ميديا + إكمال توثيق ناقص

- **سرعة `npm run validate`**: تشغيل متوازي (`npm-run-all2`) + كاش لـ
  eslint/prettier/tsc — 57 ثانية → 8 ثواني بالتشغيلة الثانية. طُبّق كمان
  على GitHub Actions CI (`actions/cache`).
- **روابط سوشال ميديا جديدة** (`src/lib/social-links.ts`): Instagram،
  Facebook، YouTube، واتساب دعم فني — بمبدأ "تظهر بس لو فيها رابط حقيقي"
  (بدون حسابات وهمية/روابط ميتة). LinkedIn اتجنّب عمداً (رأي: جمهور غير
  مناسب لمنصة تعليمية لطلاب التوجيهي).
- **إكمال توثيق ناقص**: قسم 5 (EXAM_ATTEMPTS/MISTAKES/BADGES/CERTIFICATES
  وتقييمات المعلمين) كان الكود منفّذ من زمان بس التوثيق ناقص — أُكمل.
- التفاصيل: `docs/operations/2026-09-19-ci-speed-and-social-links.md`.

## 18 سبتمبر — إصلاح شامل لتعارض المحتوى العربي/الإنجليزي

- **رسومات SVG**: 12 نص عربي ثابت بـ6 رسومات توضيحية صارت مترجمة (`useTranslation`
  - قسم `illustrations.*` جديد بملفات الترجمة).
- **مفاتيح مفقودة**: 3 نصوص بالرئيسية كانت تعتمد على `defaultValue` عربي
  لأنه المفتاح الحقيقي مش موجود بأي ملف ترجمة — أُضيفت صح.
- **الأكبر**: عنوان تبويب المتصفح بـ**48 صفحة** بعد تسجيل الدخول كان عربي
  ثابت دائمًا (بعكس الصفحات العامة يلي فيها آلية صحيحة). دالة جديدة
  `authPageHead()` بـ`seo.ts` + ترجمة إنجليزية فعلية لكل الـ48.
- **تنظيف**: حذف كود ميت (`const title/description` غير مستخدم) من 15
  صفحة عامة كان يسبب لبس أثناء التشخيص.
- التفاصيل: `docs/operations/2026-09-18-bilingual-content-audit.md`.

## 18 سبتمبر — أمان الباك اند + دليل Sentry + جاهزية دمج المعلمين/الكورسات

- **بق حرج بالإنتاج**: الموقع كان واقع بالكامل (500 على كل صفحة) بسبب فشل
  ترحيل حزمة Sentry بدالة Vercel السيرفرلس — صُلّح (`traceDeps` بـNitro +
  `import()` ديناميكي محمي بـ`server.ts`).
- **أمان الباك اند**: كل الـcontrollers (ماعدا Auth) كانت بدون أي حماية
  `[Authorize]` فعلية — أُضيفت fallback policy إجبارية + `[AllowAnonymous]`
  دقيقة على نقاط التصفّح العامة بس.
- **`MyProfileModal`**: كانت ترجع كائن فاضي 200 بدل خطأ واضح — صارت 404.
- **دليل Sentry**: `docs/operations/sentry-setup-guide.md` — خطوة بخطوة.
- **تقرير جاهزية الباك اند الجديد** (Booking/Course/Lesson/Teacher، 26
  endpoint): `docs/operations/2026-09-18-backend-integration-readiness.md`
  — اكتشاف مهم: "مسجّل مسبقاً" (recorded) مش مدعومة بالباك اند إطلاقاً.

## 16 سبتمبر (تكملة) — تطبيق التموضع التسويقي (marketplace) بالرئيسية

- فجوة حقيقية: هيرو الصفحة الرئيسية وكل بطاقات المزايا (6) ما كانت تذكر
  الكورسات/الأساتذة/الـmarketplace إطلاقاً رغم إنه `/courses` مكتوب فيها
  "سوق الكورسات" أصلاً.
- بطاقة سابعة جديدة بقسم المزايا: "أساتذة من كل مكان، بالصيغة يلي تناسبك".
- تقوية `sub` ووصف SEO لصفحة `/courses` (عربي/إنجليزي) لتذكر صراحة "محليين
  وعالميين" و3 صيغ التقديم (مباشر/وجاهي/مسجّل).
- التفاصيل: قسم 4 بـ`docs/operations/2026-09-16-competitor-analysis-and-course-data-honesty.md`.

## 16 سبتمبر — تحليل المنافسين + إصلاح صدق بيانات الكورسات

- **تحليل منافسين حقيقي**: tawjihi.app، tawjihipal.com، A+ — خلاصة: مشروعنا
  الوحيد كـ**marketplace كورسات** (الباقي محتوى مركزي)، لازم يُبرز بالتسويق.
- **إصلاح تناقض**: `PUBLIC_COURSES` كانت 6 كورسات/أساتذة **وهمية بالكامل**
  رغم عدم وجود أساتذة حقيقيين فعلاً — يخالف مبدأ المشروع بعدم عرض بيانات
  تجريبية كحقيقية. صارت مصفوفة فاضية + حالة "قريباً" صادقة (بنفس أسلوب قسم
  الآراء) بدل حالة فراغ واحدة مربكة.
- **حقل جديد**: `format` (أونلاين مباشر/وجاهي/مسجّل) — إجباري بالتحقق،
  badge بالبطاقة العامة وعمود بلوحة الأدمن.
- **فلتر فرع/مستوى جديد** بصفحة الكورسات (علمي/أدبي/...)، منفصل عن فلتر
  المادة.
- التفاصيل الكاملة: `docs/operations/2026-09-16-competitor-analysis-and-course-data-honesty.md`.

## جلسة 15 سبتمبر (تكملة ثالثة) — الشعار بالصورة الأصلية + تصحيح "من نحن"

- **الشعار**: استُبدلت الرسمة اليدوية للجلسة السابقة بـ**الصورة الأصلية
  الحقيقية** يلي بعتها المستخدم — قصّ دقيق بالاعتماد على الشفافية الحقيقية،
  تصغير فقط (بدون تكبير يسبب تبكسل)، ضغط PNG محقَّق بصرياً. الشعار الآن
  **لون ثابت** (تدرّج ذهبي) بدل `currentColor` — قرار مقصود لأنه واضح على
  الوضعين الغامق والفاتح الاثنين.
- **أصول جديدة**: `public/brand/logo-mark.png`, فافيكون (svg+ico) وأيقونات
  PWA وog-image كلها أُعيد توليدها من نفس الصورة الأصلية.
- **صفحة "من نحن"**: "مؤسس" → "مؤسس مشارك" للثلاثة + إزالة تكرار مسمّى
  أحمد/زياد الحرفي بتمييز كل وحد حسب تخصّصه الفعلي.
- التفاصيل الكاملة: `docs/operations/2026-09-15-brand-logo-real-image-and-about-fix.md`.

## جلسة 15 سبتمبر (تكملة) — تحديث الشعار والهوية البصرية

- **شعار جديد**: استبدال شخصية الماسكوت بشعار "قبعة تخرج فوق جبل/حرف A" —
  مرسوم يدوياً بأشكال هندسية نظيفة (مش استيراد SVG متتبّع آلياً)، لسا
  `fill="currentColor"` بالكامل.
- **إصلاح تفاوت حجم الشعار بين الشاشات**: أُزيلت حيلة "تجاوز الأيقونة حدود
  صندوقها" (كانت خاصة بالماسكوت القديم) — الشعار الجديد يتناسب داخل صندوقه
  بكل الأحجام.
- **فافيكون/أيقونات التطبيق**: أُعيد توليدها كلها بالشعار الجديد (نسخة مبسّطة
  بدون شرابة للفافيكون الصغير، نسخة كاملة للأيقونات الكبيرة والهيدر).
- **`og-image.svg`**: أُعيد بناؤه بالكامل — كان تصميماً قديماً غير متعلق
  بهويتنا (أيقونة كتاب بتدرّج أزرق).
- **أيقونات أزرار الهيدر**: قاعدة موحّدة جديدة (أزرار مليانة = أيقونة، روابط
  نصية = بدون) + إضافة أيقونة لزر "ابدأ مجاناً" بالنسخة المكتبية.
- التفاصيل الكاملة: `docs/operations/2026-09-15-brand-logo-refresh.md`.

## جلسة 15 سبتمبر — أدوات جودة، متغيرات بيئة، هيدرز أمان

- **باگات مصلّحة**: `useMemo` متداخلة بـ`flashcards.tsx`/`library.tsx` (مخالفة
  لقواعد React Hooks)، خطأ نوع بـ`seo.ts`، تعارض إصدارات `vite`↔`vitest`.
- **CI**: إضافة `typecheck` و`format:check` كخطوات — كانت الأنواع مش
  مفحوصة إطلاقاً قبل هالجلسة رغم وجود أخطاء حقيقية.
- **`package.json`**: سكريبتات جديدة (`typecheck`, `lint:fix`,
  `format:check`, `test:coverage`, `validate`) + `engines`.
- **`src/lib/env.ts`**: نقطة مركزية وحيدة للتحقق من متغيرات البيئة (zod)
  بدل قراءة `import.meta.env` مباشرة بـ9 ملفات متفرقة.
- **أمان**: هيدرز HTTP جديدة (`vercel.json` + `public/_headers`) — CSP,
  HSTS, X-Frame-Options... (يحتاج اختبار CSP بعد أول نشر، راجع التفاصيل).
- **تنظيف**: توحيد Acadimia→Academia (3 ملفات)، إزالة
  `vite-tsconfig-paths` لصالح دعم Vite 8 الأصلي.
- التفاصيل الكاملة (الأسباب، الجداول، التحذيرات):
  `docs/operations/2026-09-15-tooling-and-security-hardening.md`.

## Unreleased (جلسة 14 سبتمبر — ربط Wallet/Parent، RBAC، اختبارات، مراقبة)

- **Wallet**: تكامل كامل (10 endpoints) — محفظة الطالب (`/wallet`، جديدة)،
  أرباح المعلّم (`/teacher/earnings`، أُعيد بناؤها بالكامل)، مراجعة
  طلبات الأدمن (`/admin/payments`، أُعيد بناؤها بالكامل)، ترقيم صفحات
  حقيقي لسجل الحركات.
- **حساب المستخدم**: تعديل الملف الشخصي وتغيير كلمة المرور — ربط حقيقي
  كامل بـ`/settings` (ما كان موجود إطلاقًا قبل هالجلسة).
- **تسجيل/دخول حقيقي**: `/signup` و`/login` مربوطين بالكامل بكل الأدوار
  (كان بس أدمن ديمو). تصحيح مصطلحات المصفوفة (كورس لا دورة).
- **RBAC**: صلاحيات حقيقية لكل الأدوار (كانت أدمن بس) + تجميع منطقي
  للقائمة الجانبية (13→4 عناصر طالب، 10→4 معلّم، 8→3 شؤون أكاديمية).
- **ترقيم صفحات حقيقي**: المستخدمون، الثوابت، سجل المحفظة — كانوا
  يجيبوا حتى 1000 صف بطلب وحدة بدون حد فعلي.
- **باگات مؤكدة ومصلّحة** (Console/Network فعلي): `useMemo` غير مستورَد
  بلوحة الأدمن (كان يكسرها بالإنتاج)، React key مكرّر بالصفحة الرئيسية،
  `CreateEditModal?id=` فاضي يرجّع 400 (كان بيفرّغ قوائم الجنس/النوع).
- **onboarding**: قائمة "خطواتك الأولى" حقيقية (طالب/معلّم) — إشارات
  إنجاز حقيقية (رصيد محفظة)، مش بيانات وهمية.
- **نظام حركة موحّد**: 3 variants مسمّاة لـ`<Reveal>` بدل أرقام حرة.
- **دليل مصطلحات ونبرة صوت**: `docs/design/ux-guidelines.md`.
- **مراقبة**: Sentry (`@sentry/tanstackstart-react`) + PostHog مربوطين
  (يحتاجوا `npm install` وقيم `.env` حقيقية قبل الإنتاج).
- **اختبارات**: Vitest + Playwright مُعدّين لأول مرة، مربوطين بـCI
  (`npm test` + `npm audit --audit-level=high`).
- **فجوات باك اند موثّقة** (مو مصلّحة من الفرونت): بوابة ولي الأمر
  معطّلة عمليًا (لا endpoint لربط الأبناء)، `/admin/dashboard` لسا
  بيانات ديمو، تسجيل دخول الأدمن الحقيقي يفشل حاليًا (`MyProfileModal`
  يرجّع حقول null) — راجع `docs/api/frontend-integration-status.md`.

## إضافة بنية توثيق شاملة

- إضافة بنية توثيق شاملة للمنتج والمعمارية وAPI والجودة والتشغيل.
- توثيق حدود التكامل الحالية مع Academia API.
- توثيق أن route guards في الفرونت ليست authorization server-side.

## Current baseline

- الفرونت مبني على React وTypeScript وVite وTanStack Router/Start.
- المصادقة الحالية تستعمل ASP.NET Identity cookie عبر الباك إند المنشور.
